# Scraper Fixtures

Keep scrubbed, representative source responses here. The test runner never contacts a live website: it returns these files through the mocked `novel.network` bridge.

Update the JSON fixtures when replacing the default API mapper. Use `chapter.html` when adopting the optional HTML recipe. Never commit credentials, session cookies, private chapters, or personally identifiable user data.